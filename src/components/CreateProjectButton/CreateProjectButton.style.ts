import { createStyles, rem } from '@mantine/core';

export const useCreateProjectButtonStyle = createStyles(() => ({
    buttonIcon: {
        width: rem(15),
        height: rem(15),
    },
    input: {
        width: '100%',
    }
}));

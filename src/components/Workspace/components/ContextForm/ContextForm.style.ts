import { createStyles, rem } from "@mantine/core";

export const useContextFormStyle = createStyles(() => ({
    searchIcon: {
        width: rem(15),
        height: rem(15),
    },
    searchInput: {
        width: '100%',
    },
}));

import { createStyles, rem } from "@mantine/core";

export const useSearchInputStyle = createStyles(() => ({
    searchIcon: {
        width: rem(15),
        height: rem(15),
    },
    searchInput: {
        width: '100%',
    },
}));

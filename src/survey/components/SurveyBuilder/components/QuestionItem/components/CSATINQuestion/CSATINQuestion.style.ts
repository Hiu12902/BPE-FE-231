import { createStyles } from "@mantine/core";

export const useCSATINQuestionStyle = createStyles(() => ({
    option: {
        margin: "5px",
    },
    input: {
        marginTop: "15px",
        marginLeft: "-5px",
    }
}))
import { createStyles } from "@mantine/core";

export const useCESINQuestionStyle = createStyles(() => ({
    option: {
        margin: "5px",
    },
    input: {
        marginTop: "15px",
        marginLeft: "-5px",
    }
}))
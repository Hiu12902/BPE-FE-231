import { createStyles } from "@mantine/core";

export const useCreateQuestionModalStyle = createStyles(() => ({
    sectionWrapper: {
        width: '100%',
        height: '100%',
        margin: "0 0 20px 0",
    }
}))